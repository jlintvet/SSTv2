# Core HTTP
requests
urllib3

# Numerical / array
numpy
pandas
xarray

# NetCDF (BathyTileGenerator, VIIRSHourlyBundler)
netCDF4

# Image processing (BathyTileGenerator)
Pillow

# AWS S3 (BathyTileGenerator)
boto3

# HTML scraping (scraper.py)
beautifulsoup4

# Copernicus Marine — optional soft dep in DailyChlorophyllandSeaColorRetrieval.py
# Script auto-falls-back to ERDDAP if missing, but include for full CMEMS support
copernicusmarine
